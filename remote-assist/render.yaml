services:
  - type: web
    name: tuctuc-remote
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ACCESS_TOKEN
        value: tuctuc-remote-2026
