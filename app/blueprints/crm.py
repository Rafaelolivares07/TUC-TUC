from flask import Blueprint

bp = Blueprint('crm', __name__)

# TODO: migrar rutas /chat, /api/vendedor, /api/tercero, /api/chat


def job_verificar_recordatorios():
    """Tarea APScheduler — verifica recordatorios pendientes"""
    pass
