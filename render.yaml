services:
  - type: web
    name: TUC-TUC
    env: python
    buildCommand: apt-get install -y ffmpeg && pip install -r requirements.txt
    startCommand: gunicorn main:app --timeout 120 --workers 1 --preload